import { Button } from "../Button";

const Footer = () => {
    return (
        <div className="flex flex-col gap-6">
            <Button
                text="Add new task"
                onClick={() => {}}
                variant="primary"
            />
        </div>

    )
}

export default Footer;